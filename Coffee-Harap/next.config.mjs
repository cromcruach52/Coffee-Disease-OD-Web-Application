/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'assets.aceternity.com'], // Add any other domains you use for images here
      },
};

export default nextConfig;
